{
  "name": "spacehouse",
  "version": "1.0.0",
  "description": "SpaceHouse Configurator - Full Stack Application",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "postinstall": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
